Scripts and files to generate a Debian binary package from a source archive.

Usage:

  1. Download source package to root/timeline-x.y.z.zip
  2. Extract to root/timeline-x.y.z
  3. Symlink the debian directory
     cd root/timeline-x.y.z
     ln -s <path-to-deb-repo>/debian
  4. Run ./debian/scripts/build
